tasks:
  dev:
    description: build extension for development and watch
    options:
      cache: false
    command: bun scripts/dev.ts

  grammar:
    description: build the grammar files
    options:
      cache: false
    command: bun scripts/grammar.ts

  publish:
    description: build and publish the extension
    options:
      cache: false
    command: bun scripts/publish.ts
